[project]
name = "ssh-proxy-client"
version = "0.1.0"
description = "SSH client proxy for Kubeflow GPU pods"
requires-python = ">=3.10"
dependencies = [
    "websockets",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
